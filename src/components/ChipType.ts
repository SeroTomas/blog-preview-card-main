import { Neutral, Primary } from "@/utilities";
import styled from "styled-components";

export const ChipType = styled.span`
    background-color: ${Primary.yellow};
    color: ${Neutral.black};
    font-size: 1.3rem;
    font-weight: 800;
    padding: 5px 10px 5px 10px;
    width: fit-content;
    border-radius: 3px;
`