import { Neutral } from "@/utilities";
import styled from "styled-components";

export const Text = styled.p`
    font-size: 1.3rem;
    color: ${Neutral.grey};
    font-weight: 600;
    line-height: 2rem;
`