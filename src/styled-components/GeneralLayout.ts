import styled from "styled-components";
import { Primary } from "@/styled-components";

export const GeneralLayout = styled.div`
    display: grid;
    place-items: center;
    background-color: ${Primary.yellow};
`